#pragma once

const float H = 10.0f;
const float L = 10.0f;
const float R = 5.0f;

const int m = 10;
const int n = 10;

const int nummIterations = 10;

const double phiLower	= 0.0f;
const double phiHigher	= 1.0f;

const double deltaPhi = (phiHigher - phiLower) / (m + 1);

const float sigma = 0.2;